// import App from './App.svelte';

// const app = new App({
// 	target: document.body,
// });

// export default app;


import Launch from './Launch.svelte';

const app = new Launch({
	target: document.body,
});

export default app;